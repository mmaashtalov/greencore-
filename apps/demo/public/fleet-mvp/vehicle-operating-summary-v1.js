const VOS_CFG={url:'https://tikjmiyrhkcjrxjylmqb.supabase.co',key:'sb_publishable_clr5P9USk7b63MajJmmr9A_Iz0wi_0F',version:'2026.08.16-operating1'};
const VOS_SESSION='fleet_mvp_session_v2',VOS_VEHICLE='fleet_mvp_current_vehicle';
function vosSession(){try{return JSON.parse(localStorage.getItem(VOS_SESSION)||'null')}catch{return null}}
function vosSaveSession(s){if(s)localStorage.setItem(VOS_SESSION,JSON.stringify(s))}
async function vosRefresh(){const s=vosSession();if(!s?.refresh_token)throw new Error('Сессия истекла.');const r=await fetch(`${VOS_CFG.url}/auth/v1/token?grant_type=refresh_token`,{method:'POST',headers:{apikey:VOS_CFG.key,'Content-Type':'application/json'},body:JSON.stringify({refresh_token:s.refresh_token})});const d=await r.json().catch(()=>({}));if(!r.ok||!d.access_token)throw new Error(d.message||d.msg||'Сессия истекла.');d.obtained_at=Date.now();vosSaveSession(d);return d}
async function vosRpc(id,retry=true){let s=vosSession();if(!s?.access_token)return null;const r=await fetch(`${VOS_CFG.url}/rest/v1/rpc/get_vehicle_operating_summary`,{method:'POST',headers:{apikey:VOS_CFG.key,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({p_vehicle_id:id})});if(r.status===401&&retry){await vosRefresh();return vosRpc(id,false)}if(!r.ok)return null;const t=await r.text();return t?JSON.parse(t):null}
function vosEsc(v=''){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
function vosNum(v,d=1){if(v===null||v===undefined||v==='')return'—';const n=Number(v);return Number.isFinite(n)?n.toLocaleString('ru-RU',{maximumFractionDigits:d}):'—'}
function vosMoney(v){if(v===null||v===undefined||v==='')return'—';const n=Number(v);return Number.isFinite(n)?`${n.toLocaleString('ru-RU',{maximumFractionDigits:2})} ₽`:'—'}
function vosMetric(value,label,sub=''){return `<div class="metric"><div class="metric-value">${value}</div><div class="metric-label">${vosEsc(label)}</div>${sub?`<div class="hint">${vosEsc(sub)}</div>`:''}</div>`}
function vosSection(d){const m=d.movement||{},f=d.fuel||{},s=d.service||{},money=d.money||{},c=d.completeness||{},cm=d.cost_model||{};const hasHistory=Number(m.finalized_waybills||0)>0||Number(s.maintenance_completed||0)>0||Number(s.repair_count||0)>0;if(!hasHistory)return null;const issues=[];if(Number(f.missing_cost_transactions||0)>0)issues.push(`Без стоимости: ${Number(f.missing_cost_transactions)} заправок`);if(Number(s.maintenance_missing_cost||0)>0)issues.push(`Без стоимости: ${Number(s.maintenance_missing_cost)} ТО`);if(Number(s.repair_unpriced_work_items||0)>0)issues.push(`Без стоимости: ${Number(s.repair_unpriced_work_items)} ремонтных работ`);if(Number(s.closed_repairs_without_work_log||0)>0)issues.push(`Без журнала работ: ${Number(s.closed_repairs_without_work_log)} закрытых ремонтов`);const incomplete=c.status==='partial';const section=document.createElement('section');section.className='section vos-section';section.dataset.vosSection=VOS_CFG.version;const known=money.recorded_known_cost_available?vosMoney(money.recorded_known_cost):'—';const knownSub=money.recorded_known_cost_available?(money.recorded_known_cost_complete?'все записанные прямые расходы заполнены':'только известные суммы'):'денежных сумм пока нет';const fuelCost=f.monetary_cost_available?vosMoney(f.recorded_cost):'—';section.innerHTML=`<div class="section-head"><div><h2>Эксплуатация и затраты</h2><p>${vosEsc(d.scope?.label||'За записанную историю')}</p></div><span class="badge ${incomplete?'warning':c.status==='recorded_complete'?'success':''}">${vosEsc(c.label||'')}</span></div><div class="card"><div class="grid grid-4">${vosMetric(`${vosNum(m.confirmed_mileage_km,0)} км`,'Подтверждённый пробег',`${Number(m.finalized_waybills||0)} закрытых ПЛ`)}${vosMetric(f.average_l_per_100km===null||f.average_l_per_100km===undefined?'—':`${vosNum(f.average_l_per_100km,2)} л/100 км`,'Фактический расход',`${vosNum(f.actual_consumption_l,0)} л израсходовано`)}${vosMetric(fuelCost,'Заправки, ₽',`${Number(f.priced_transactions||0)} из ${Number(f.cost_transactions||0)} с ценой`)}${vosMetric(known,'Записано расходов',knownSub)}</div>${money.recorded_known_cost_per_1000km!==null&&money.recorded_known_cost_per_1000km!==undefined?`<div class="hint" style="margin-top:10px">Записанные прямые расходы: ${vosMoney(money.recorded_known_cost_per_1000km)} на 1 000 км. Это не полная себестоимость владения.</div>`:''}${issues.length?`<div class="alert warning" style="margin-top:12px"><div>!</div><div><div class="alert-title">Данные по расходам неполные</div><div class="alert-sub">${vosEsc(issues.join(' · '))}</div></div></div>`:''}<div class="hint" style="margin-top:10px">${vosEsc(cm.reason||'Показаны только фактически записанные данные.')}</div></div>`;return section}
async function vosDecorate(){const main=document.querySelector('.main');if(!main||main.querySelector('[data-vos-section]')||main.dataset.vosLoading==='1')return;if(!main.querySelector('[data-ec-action="edit-vehicle"]'))return;const id=sessionStorage.getItem(VOS_VEHICLE);if(!id)return;main.dataset.vosLoading='1';try{const d=await vosRpc(id);if(!d||!main.isConnected)return;const section=vosSection(d);if(!section)return;const repair=main.querySelector('.vrc-section'),history=main.querySelector('.vehicle-history-section'),maint=main.querySelector('.svc-vehicle-maintenance');if(repair)repair.before(section);else if(history)history.before(section);else if(maint)maint.before(section);else main.appendChild(section)}finally{if(main.isConnected)delete main.dataset.vosLoading}}
let vosQueued=false;function vosSchedule(){if(vosQueued)return;vosQueued=true;queueMicrotask(()=>{vosQueued=false;vosDecorate()})}new MutationObserver(vosSchedule).observe(document.documentElement,{childList:true,subtree:true});window.addEventListener('fleet:role-ux-ready',vosSchedule);vosSchedule();
