const UXLOGIN_VERSION='2026.08.15-login1';
function uxloginDecorate(){const form=document.getElementById('loginForm');if(!form||form.dataset.uxlogin===UXLOGIN_VERSION)return;form.dataset.uxlogin=UXLOGIN_VERSION;const password=form.elements.password,email=form.elements.email,error=document.getElementById('loginError');if(error){error.setAttribute('role','alert');error.setAttribute('aria-live','assertive')}
  if(password&&!password.parentElement.querySelector('.uxlogin-password-wrap')){const field=password.parentElement;const wrap=document.createElement('div');wrap.className='uxlogin-password-wrap';password.before(wrap);wrap.appendChild(password);const toggle=document.createElement('button');toggle.type='button';toggle.className='uxlogin-toggle';toggle.setAttribute('aria-label','Показать пароль');toggle.setAttribute('aria-pressed','false');toggle.textContent='Показать';toggle.addEventListener('click',()=>{const show=password.type==='password';password.type=show?'text':'password';toggle.textContent=show?'Скрыть':'Показать';toggle.setAttribute('aria-label',show?'Скрыть пароль':'Показать пароль');toggle.setAttribute('aria-pressed',String(show));password.focus()});wrap.appendChild(toggle);const caps=document.createElement('div');caps.className='uxlogin-caps';caps.hidden=true;caps.textContent='Включён Caps Lock';field.appendChild(caps);const updateCaps=e=>{if(typeof e.getModifierState==='function')caps.hidden=!e.getModifierState('CapsLock')};password.addEventListener('keydown',updateCaps);password.addEventListener('keyup',updateCaps);password.addEventListener('blur',()=>caps.hidden=true)}
  requestAnimationFrame(()=>{if(email?.value&&password)password.focus();else email?.focus()})}
let uxloginPending=false;function uxloginSchedule(){if(uxloginPending)return;uxloginPending=true;queueMicrotask(()=>{uxloginPending=false;uxloginDecorate()})}new MutationObserver(uxloginSchedule).observe(document.documentElement,{childList:true,subtree:true});uxloginSchedule();