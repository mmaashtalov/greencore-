const UXFR_VERSION='2026.08.15-fresh1';function uxfrTime(d=new Date()){return new Intl.DateTimeFormat('ru-RU',{hour:'2-digit',minute:'2-digit'}).format(d)}function uxfrDecorate(){const main=document.querySelector('.main');const head=main?.querySelector('.page-head');const title=head?.querySelector('.page-title')?.textContent?.trim();if(!main||!head||!title||main.querySelector('form')||title==='Вход')return;let chip=head.querySelector('.uxfr-chip');if(!chip){chip=document.createElement('span');chip.className='uxfr-chip';const host=head.firstElementChild||head;host.appendChild(chip);main.dataset.uxfrAt=String(Date.now())}const at=Number(main.dataset.uxfrAt)||Date.now();chip.classList.toggle('offline',!navigator.onLine);chip.textContent=navigator.onLine?`Экран обновлён в ${uxfrTime(new Date(at))}`:'Офлайн · показаны доступные данные'}let uxfrPending=false;function uxfrSchedule(){if(uxfrPending)return;uxfrPending=true;queueMicrotask(()=>{uxfrPending=false;uxfrDecorate()})}new MutationObserver(uxfrSchedule).observe(document.documentElement,{childList:true,subtree:true});window.addEventListener('online',uxfrDecorate);window.addEventListener('offline',uxfrDecorate);uxfrSchedule();