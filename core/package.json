{
  "name": "@greencore/core",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Hardware-agnostic greenhouse control engine with persistent controller routing, safety rules, HTTP API and controller emulation.",
  "scripts": {
    "test": "node --test",
    "demo": "node src/demo.js",
    "start": "node src/server.js",
    "emulator:controller": "node src/controller-emulator-cli.js"
  },
  "engines": {
    "node": ">=20"
  }
}
