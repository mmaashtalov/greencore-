{
  "name": "@greencore/core",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Hardware-agnostic greenhouse control engine with persistent controller routing, safety rules, HTTP API and emulation.",
  "scripts": {
    "test": "node --test",
    "demo": "node src/demo.js",
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
