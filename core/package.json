{
  "name": "@greencore/core",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Hardware-agnostic greenhouse runtime with persistent controller routing, automatic safety evaluation and a deterministic digital twin.",
  "scripts": {
    "test": "node --test",
    "demo": "node src/demo.js",
    "start": "node src/server.js",
    "emulator:controller": "node src/controller-emulator-cli.js"
  },
  "engines": {
    "node": ">=20"
  }
}
