{
  "name": "@greencore/core",
  "version": "0.12.0",
  "private": true,
  "type": "module",
  "description": "Hardware-agnostic greenhouse runtime with rate limits, bounded simulations, SSE live stream, scoped API authorization and SQLite analytics.",
  "scripts": {
    "test": "node --test",
    "demo": "node src/demo.js",
    "start": "node src/server.js",
    "emulator:controller": "node src/controller-emulator-cli.js",
    "scenarios": "node src/scenario-runner-cli.js",
    "faults": "node src/fault-campaign-cli.js"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
